import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  inputContainer: {
    alignItems: "center",
  },
  warningText: {
    marginTop: 10,
    marginLeft: 10,
    color: "red",
  },
});
