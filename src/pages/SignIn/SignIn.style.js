import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  header: {
    alignItems: "center",
    gap: 20,
    marginBottom: 40,
  },
  icon: {
    width: 100,
    height: 100,
    borderRadius: 10,
  },
  headerText: {
    fontSize: 20,
  },
  titleText: {
    fontSize: 14,
  },
});
