import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    paddingTop: 100,
  },
  medFlatList: {
    marginTop: 50,
    paddingBottom: 50,
  },
});
